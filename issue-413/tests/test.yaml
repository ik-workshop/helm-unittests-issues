suite: MyTemplate test
templates:
  - template.yaml
capabilities:
  apiVersions:
    - monitoring.coreos.com/v1
tests:
  - it: with monitoring disabled, ServiceMonitor not created
    capabilities:
      apiVersions:
    asserts:
      - hasDocuments:
          count: 0
