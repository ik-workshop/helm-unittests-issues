---
templates:
  - templates/deploy.yaml
tests:
  - it: selects the correct kind
    documentSelector:
      path: kind
      value: Deployment
    asserts:
      - equal:
          path: kind
          value: Deployment
