---
suite: "test paradox"
templates:
  - templates/should-fail.yaml
tests:
  - it: requires a slackAlertChannel
    set:
      slackAlertChannel: ~
      datadog:
        monitors:
    asserts:
      - failedTemplate:
          errorMessage: A valid .Values.slackAlertChannel is required
