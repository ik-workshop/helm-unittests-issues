suite: Override values in tests
templates:
  - template.yaml
release:
  name: my-release-name
  namespace: my-namespace
set:
  disableAutoApproval: true
tests:
  - it: test me
    asserts:
      - hasDocuments:
          count: 1
